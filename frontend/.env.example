# Browser-visible API origin. Use same-origin routing in production.
NEXT_PUBLIC_API_BASE_URL=
# Server-only API rewrite destination for local development.
API_SERVER_URL=http://localhost:4000
# Optional Next.js CI marker; leave blank unless a deployment policy requires it.
CI=
