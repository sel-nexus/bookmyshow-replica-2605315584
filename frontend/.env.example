# Browser-visible API origin. Leave empty for same-origin production deployments.
NEXT_PUBLIC_API_BASE_URL=
# Server-only development rewrite destination for the Express API.
API_SERVER_URL=http://localhost:4000
